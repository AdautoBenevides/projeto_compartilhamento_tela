{
  "name": "screen-share-desktop",
  "version": "1.0.0",
  "description": "Desktop screen sharing application",
  "main": "dist/main.js",
  "scripts": {
    "copy-ui": "node scripts/copy-ui.js",
    "dev": "tsc && npm run copy-ui && electron dist/main.js",
    "build": "tsc && npm run copy-ui",
    "start": "electron dist/main.js"
  },
  "dependencies": {
    "electron-is-dev": "^2.0.0",
    "socket.io-client": "^4.7.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "electron": "^28.0.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.3.2"
  }
}
